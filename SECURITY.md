# 安全策略

## 报告漏洞

请通过 GitHub 的[私密漏洞报告](https://github.com/Nobilta/pure-browser/security/advisories/new)提交安全问题
（仓库的 Security → Report a vulnerability），避免在公开 Issue 中披露利用细节。
如果该入口不可用，可以开 Issue 提醒维护者启用私密报告，先不要附上漏洞详情。

报告中请尽量提供：

- 应用、Android 和 WebView 的版本。
- 复现步骤或最小页面；涉及恶意网页时，最好提供可离线复现的 HTML。
- 可能的影响，例如读取其他站点数据、绕过权限，以及是否需要用户操作。
- 问题是否已公开披露。

维护者会核实影响并安排修复。修复发布后可在版本说明中致谢；希望匿名时请在报告中注明。

## 支持范围

安全修复面向最新正式版。旧版本出现的问题，请尽可能确认最新版是否仍然受影响。
涉及 WebView / Chromium 的缺陷，也请报告给 [Chromium 问题跟踪](https://issues.chromium.org/issues/new?component=1456367)；
本项目能做的修复取决于问题所在层，部分情况需要更新系统 WebView。

## 与安全有关的行为

以下说明帮助判断预期行为。实际表现偏离这些说明，或这些机制存在可利用的缺陷时，仍欢迎报告。

- **用户脚本**运行在网页环境中，没有隔离执行环境。当前不提供 `GM_xmlhttpRequest` 或任意原生网络、文件接口；仅安装你信任的脚本。
- **广告过滤**使用 Rust 网络规则引擎和网页元素隐藏规则，只支持部分规则语法，不能保证拦截所有跟踪或恶意内容。
- **无痕模式**不隐藏公网 IP。支持独立 WebView Profile 时隔离会话；不支持时会提示共享存储，退出清理也会影响普通模式的 Cookie。
- **截屏与最近任务预览**会照常显示无痕页面。主动保存的书签和下载也会保留。
- **下载凭据**来自当前网页及其 Profile。Cookie 不持久化到下载记录，重定向只向原 origin 发送 Cookie，并拒绝 HTTPS 降级。
- **应用更新**只从允许的 GitHub HTTPS 地址下载，安装前检查大小、SHA-256、包名、版本、最低 SDK、ABI 和签名。最终由 Android 安装器确认。

完整行为说明见[功能介绍](FEATURES.md)。正式签名密钥由维护者在本机保管，不进入仓库或 CI；
发布和签名要求见[发布指南](RELEASING.md)。
