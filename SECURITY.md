# 安全策略

## 报告漏洞

**请不要用公开 Issue 报告安全问题。** 使用 GitHub 的
[私密漏洞报告](https://github.com/Nobilta/pure-browser/security/advisories/new)
（Security → Report a vulnerability），或以邮件联系维护者（见仓库主页）。

报告里请包含：

- 受影响版本（设置 → 关于 里的版本号或 APK 的 `versionCode`）、Android 版本、WebView 版本；
- 复现步骤或最小页面；如果涉及恶意网页，请给出可离线复现的 HTML；
- 影响判断（例如：能否读取其他站点的数据、能否绕过权限、是否需要用户交互）；
- 是否已经在公开渠道披露过。

我们会在确认后尽快回复，并在修复发布后于 Release 说明里致谢（如果你希望匿名请注明）。

## 支持范围

- 只支持**最新发布版本**上的问题；旧版本的问题请先确认在最新版是否仍然存在。
- 只处理本应用自身的代码。WebView / Chromium 层面的问题请同时上报
  [Google 的 WebView 问题跟踪](https://issues.chromium.org/issues/new?component=1456367)，我们只能在上层规避。

## 设计上的安全边界

这些是**已知且有意为之**的边界，不算漏洞：

- **广告过滤与用户脚本运行在网页环境**，不是隔离世界；不提供 `GM_xmlhttpRequest` 或任意原生网络/文件接口。
- **无痕模式不隐藏公网 IP**，也不阻止网站通过其他手段识别会话；清理能力受 WebView Provider 限制。
- **无痕不屏蔽截屏、也不隐藏最近任务预览**：截图与任务切换预览会包含无痕页面内容，这是有意的行为。
- **增强播放器**只在全屏时接管当前播放器，失败即回退网页控件；不复制媒体、不改写 `src`。
- **应用更新**只接受 HTTPS 与 GitHub 资产域名，并在交给系统安装器前复核 SHA-256、包名、版本、
  最低 SDK、ABI 与当前安装签名；但它最终仍由系统安装器与用户确认，不是静默安装。
- **下载**使用网页凭据与当前 Profile 的 Cookie，只向原 origin 发送重定向凭据。

## 我们不会做的事

- 不为特定网站绕过付费墙、DRM 或访问控制；
- 不接受要求扩大权限而不说明用途的改动；
- 不在 CI 或仓库中保存签名密钥。
