# 更新日志

## 0.6.0 - 2026-09-11

- 增强全屏自动接管已加载的视频及网站自定义容器，不再要求原生 controls 或按域名排除。HTTP(S) 与 Blob/MSE 保留原视频、进度和加载状态；临时隐藏网站控制层，支持切回，隔离失败恢复网页。
- 修正 Rust 第三方规则的站点身份，加入 Public Suffix List、IDN/IP/公共托管租户边界；保留 JRS 桌面模式修复和来源权限隔离。
- 清理数据可选 Profile、类型和历史时间，增加单站清理及结果反馈；外部应用跳转按来源询问/允许/阻止，连续 renderer 崩溃停止自动重载并提供恢复操作。
- 无痕持续显示实际隔离状态，保护截图和最近任务预览，默认不使用普通历史建议；旧 WebView 的共享存储清理边界明确显示。
- 常用菜单入口前置，补充文字标签、返回历史、标签长按/可选滑动、顶部/底部地址栏、设置搜索和 Chromium 焦点下的键盘快捷键。
- 数据库升级至 v3，支持书签文件夹/排序/移动/批量操作、层级 HTML 导入导出和域名/前缀优先建议；标签支持命名分组和网站桌面快捷方式。
- 增加版本化本地备份、SAF 导出/预览、合并冲突处理、选择性权限恢复、失败回滚及独立恢复进程；恢复的用户脚本默认停用。
- 改善阅读正文评分、段落/代码/链接保真和阅读位置；最近普通标签可驻留一个 WebView，保留表单、SPA 和滚动，离开时暂停媒体；低内存设备不驻留。
- 下载增加全局/每主机任务与连接预算、排队、网络策略、有限重试、保存阶段、速度/剩余时间及通知操作；日志改用有界缓冲和可见时批量发布。
- 接入系统拍照/录像上传、Autofill 与 WebAuthn。浏览器通行密钥声明并检查网站来源权限，修复现代 WebView 请求时缺少权限的异步闪退；无痕关闭凭据访问。
- 增加画中画、系统媒体控制和可选后台播放，统一窗口媒体所有权；无痕隐藏时暂停。避免重复申请音频焦点打断 Chromium，关闭其他窗口不终止当前播放。
- 修复现代 WebView 提前消费返回键/手势导致锁定视频直接退出全屏；全屏窗口按生命周期注册自己的系统返回处理，先解锁再退出。
- 增加第二个独立普通窗口及配置重建状态保留；两窗口共享普通 Profile，双窗口与无痕互斥。新增 Google Cast 设备选择、显式发送和播放控制，保留独立 DLNA 路径。
- 用户脚本预解析匹配规则，支持有界 `@resource` 下载、文本/二进制资源 API 与备份；过滤排错显示按需命中解释，并支持临时单站停用。
- Android JNI 对照后过滤 crate 使用 `opt-level=3`，其余保持体积优化；增加生成输入、并发等价和 AddressSanitizer 检查。统一 Compose 依赖图并锁定 Gradle 版本和产物校验和。
- 翻译与跨设备同步按用户决定暂不实现；完整网页归档、跨域脚本网络和隔离执行保留明确设计边界。真实账号登录、实体投屏及低端真机性能未以模拟器结果替代。

## 0.5.2 - 2026-09-10

- 修复 `m.jrs16.com` 开启桌面模式后反复重载和菜单开关来回变化：同站裸域、m.、mobile.、www. 共用桌面偏好，避免跳转时交替恢复移动和桌面 UA。
- 只有桌面偏好共用；权限、JavaScript、图片、Cookie、过滤和文字缩放仍按来源隔离，不同协议和端口分别管理。既有配置无需迁移或清除。
- 从任一入口关闭或重置桌面模式会在同一次写入中更新已有入口；网站设置显示实际生效值，无痕修改保留在当前会话。
- 相同 UA 不重复写入；视口脚本只在新文档完成后执行，避免导航中改写上一个页面。
- 新增 10 项 Android 单元测试，以及验证 JavaScript/HTTP 重定向、请求 UA、视口、开关稳定、刷新、重启和来源隔离的模拟器专项；支持额外验证实际 JRS 网站。

## 0.5.1 - 2026-09-10

- 菜单使用明确的路由栈，设置、书签、历史、下载、网站设置、离线文章和开发工具返回原菜单，再返回关闭菜单；网页操作按行为关闭整条路径。
- 为每次弹层显示绑定回调身份，旧动画、重复点击和迟到关闭不能操作新页面；只挂载栈顶并保留父级滚动位置与设置分类。
- 设置管理弹层打开时禁用父级返回；选项关闭有代次保护。重建、后台恢复保留来路，外部网址清理临时层级。
- 菜单拖动手柄避开系统状态栏；菜单操作重置退出确认，防止先前网页返回计时导致误退出。
- 新增 10 项导航状态测试和真实输入专项，覆盖窗口回收、24 轮快速往返、4 组立即返回、重建及外部导航后的网页操作。
- 删除无调用的四组旧 JNI 实验、重复等待脚本、历史摘要和图标候选；Gradle 与独立 Rust 构建共用脚本，Cargo 依赖从 184 项缩至 32 项，保留版本不变。
- 源码整理到 master，清理其他本地分支及旧产物，只保留最新 APK、必要验证记录和实际使用的源码/夹具；基准不再依赖备份分支。

## 0.5.0 - 2026-09-10

- 元素隐藏规则从 Kotlin 迁到 Rust，使用域名后缀索引、selector ID 去重和有界缓存；80 个站点 CSS 输出一致，主机冷查询中位耗时由约 1.406 ms 降到 0.401 ms，不等同于整页提速。
- 新增按 origin 管理的网站设置与相机/麦克风/定位/受保护媒体权限，分离网站同意与 Android 授权，导航取消迟到请求，无痕决定只留内存。
- 标签关闭支持撤销与最多 20 条最近关闭记录，普通网页跨重启恢复，清历史同步清列表和撤销提示。
- 新增 SAF Netscape HTML 书签导入/导出：Rust 有界解析、预览确认、非法地址拒绝、重复保留、SQLite 单事务与完整导出预校验。
- 新增原生阅读模式、字体调节、复制和离线文章，使用原子有界存储；接入 Android 打印及保存 PDF。
- 下载支持暂停、继续和进程恢复，保留分段并校验强实体标识、总长度和最终 URL；每任务锁跨 Job 更替，避免暂停/继续并发写入；Cookie 不落盘，普通重试读取当前凭据，私密任务不读取普通 Profile。
- DLNA 增加状态/进度/音量查询和播放、暂停、停止、Seek/音量控制；动作串行、后台停轮询、未知状态不冒充已播放。
- 网站偏好和阅读仓库由 Application 共享，避免 Activity 重建后的多写锁；阅读标题/系统栏和大字体横屏站点布局修正，清理废弃资源与英文计数文案。
- 修复系统文件选择器期间进程被结束再启动后，退出浏览器遗留最近任务和旧选择器；按当前 AppTask 移除任务，新增实际回归。
- 修复外部打开/分享的网址被开发工具面板遮挡；无网址的应用切换仍保留当前面板，新增两类外部 Intent 及切回应用的触摸回归。
- 新全屏视图等待本轮播放状态，避免沿用上一轮缓存的全屏标记提前接管；增加后台恢复后连续 Blob 全屏进出回归。
- 阅读窗口在就绪后应用完整窗口配置及安全边距，修正 Android 10 的系统栏遮挡、底层页面露出和 Android 14 的重复留白；布局回归核对控件与正文区域的真实边界。
- Android 测试真实构建/加载 host JNI；新增能力、文件导入导出、进程恢复、大字体布局回归与按 APK 哈希分阶段的统一 runner；运行时依赖保持不变。


## 0.4.1 - 2026-09-10

- 全模块复查并交付优化版本；保留 0.4.0 完整源码和验证证据作为回滚基线。
- Rust 广告过滤共用域名/必要字面量索引，去重候选 bucket，非锚定通配符改为单次 DP；修正 match-case 大写域名索引。增加相同完整规则集的可复现基准和参考算法等价性测试。
- 脚本 id 只计算一次，GM 值变化仅重新注册对应脚本；过滤订阅和脚本持久化复用原子 UTF-8 写入。
- 媒体匹配复用相同播放 hints，每次更新每个 URL 只解析一次；修正编码路径混淆并统一默认端口。
- 下载模型从 UI 下沉到 download 层，合并失败/暂停操作布局，修正状态图标；记录每条只序列化一次，串行发布进度快照。
- 下载显式处理有限重定向，跨 origin 不转发原站 Cookie；拒绝 HTTPS 降级和未经请求的 206 部分文件。
- 开发工具、下载和投屏仅在可见时订阅对应动态状态；删除重复过滤设置路由及安全状态来源。
- 书签/历史写入成功后才更新界面；修正删除提前报成功、数据库异常逃逸、无痕收藏状态，以及导航后的查找残留和旧加载错误。
- 用户忽略 HTTPS 证书错误后持续显示危险连接，刷新/同站重访保留警告；有效期文案不再被误解为信任校验。新增本地自签名 TLS 的模拟器回归。

## 0.4.0 - 2026-09-09

- 增加常用油猴脚本兼容层：URL/文件/代码导入、安装预览、匹配与执行时机、常用 GM API、持久存储、依赖与手动来源更新；旧 WebView 明确降级，无痕禁用。
- 内置 EasyList、EasyPrivacy、EasyList China 官方完整快照，支持自定义订阅、逐条/全部更新、条件请求、失败保留旧规则和每周自动更新。
- 基础元素隐藏与例外和 Rust 网络规则共同工作，公开实际支持的语法边界。
- 统一 MD3 主题色、容器、字型、圆角和触摸区域；网页提示、HTTP 登录、倍速改用 MD3，开发工具支持浅深主题。
- 开发工具改用标准主标签与标题栏，修正系统栏留白、横屏键盘遮挡和切换标签丢失输入；源码按需读取并丢弃过期结果。底部弹层的系统栏图标跟随应用主题。
- 广告规则匹配补齐端口与主机通配符边界，并为常见字面量规则增加快速路径，减少视频请求排队。
- 检测媒体后右下角显示单一投屏按钮，列表标记当前播放来源；标准视频安全接管，自定义网站播放器保持单一控件层。
- 修复从直播列表以新标签打开播放器后，清理旧 WebView 导致新标签的媒体回调失效、当前播放标记及倍速入口不更新的问题。
- 弹窗使用全新 WebView，交接后才释放旧实例并重新注册媒体/用户脚本，修复连续新窗口崩溃和跨域播放器无法回传状态的问题。
- 导航会使旧页面的媒体请求失效，防止仍在过滤队列中的直播地址重新出现在下一页的投屏列表。
- 保留长按倍速、双击、进度/音量/亮度手势，精简播放提示；修复关闭投屏/倍速弹层后的控件隐藏计时。
- DLNA 根据格式生成 MIME、兼容服务版本、检测 SOAP Fault，防止重复发送和过期结果误标连接。
- 构建脚本从产物元数据获取版本，强制签名检查；新增脚本、过滤和 DLNA 测试以及可复现的模拟器夹具。

### 设置返回修复

- 统一设置工具栏、系统返回键和边缘手势的层级；宽屏二级页返回分类列表，只有设置首页返回才关闭设置。
- 宽屏仅在选中分类后显示双栏，分类导航和详情的返回按钮使用相同逻辑。
- 提前注册 Activity 兜底返回处理，确保配置重建时设置内部返回仍优先执行。
- 增加已签名 APK 的设置返回回归，覆盖六类设置、选项弹层、过滤列表、后台恢复、字体重建和横竖屏。

## 0.3.1 - 2026-09-08

- 长按导航首页快捷入口打开统一编辑窗口，可修改标题、地址和图标，删除入口一并放入窗口。
- 支持系统文件选择器导入图片和使用文字图标，图片按方向解码并限制为最长边 96px。
- 编辑保留 ID、创建时间和排列位置；重复地址不覆盖其他入口，取消保留原数据。
- 自定义图标不会被后续网页 favicon 覆盖；图标文件替换失败时保留原图。
- 保留删除前确认，编辑和删除均不影响书签；补齐三语言、数据测试与模拟器触摸回归。
- 保存失败回滚快捷入口信息，图片改为先写新文件、成功后清理旧图，删除写入失败保留原图。
- 正式启用「青叶 P」应用图标，包含自适应与主题单色资源；设计目录保留旧图标快照与对比预览。
- 移除网页右下角倍速和投屏悬浮按钮；入口集中在菜单和增强全屏控制栏。
- 增强控件仅接管支持交接的标准视频全屏；YouTube 与自定义容器保留网页控件，不叠加原生控件层。
- 固定全屏控制目标并校验交接能力；退出时恢复网页控件，遥测不再唤醒已隐藏的播放栏。
- 标准视频接管期间临时抑制 WebView 强制显示的内置控件，仅影响目标元素，退出后移除样式。
- 全屏内直接打开投屏选择窗口，取消保留全屏；没有候选时准确提示未检测到直接媒体地址。
- 全屏窗口重新获得焦点时恢复系统栏隐藏状态，修复旧系统关闭弹层后的按钮遮挡。
- 从当前视频已加载的地址补充嗅探遗漏的媒体候选，保留签名参数；Blob 和无关资源线索不作为直接来源。
- 增加播放器/投屏方案，明确 DLNA 候选限制及后续远端会话设计，不宣称支持 YouTube 直投。

## 0.3.0 - 2026-09-07

- 新增网页链接/图片长按菜单，支持前台或后台打开、复制、系统分享及图片下载。
- 后台标签延迟加载；标签概览支持搜索、当前标记、缩略图和批量关闭确认。
- 书签/历史按 50 条分页，搜索覆盖整个数据库，支持取消旧查询和加载失败重试。
- 书签可从列表直接编辑，保留 ID/创建时间，重复网址保存失败不覆盖原记录；历史按日期分组。
- 主菜单顶部集中页面操作图标，删除多余说明；三语言资源同步，补齐标签按钮读屏名称。
- 移除重复缩略图编码和默认 Rust cache 库，修复恢复标签时保存中间空白状态的问题。
- WebView 归还池时清除长按/查找监听器，避免持有旧 Activity；不主动回收仍可能被绘制的位图。
- 按产品选择不增加最近关闭记录，重访页面统一使用历史记录。
- 新增数据库、分页、上下文目标及标签单元测试，以及签名 APK 的真实触摸回归脚本。

## 0.2.0 - 2026-09-07

- 最低系统降至 Android 10（API 29），Release 继续提供 arm64-v8a；target/compile SDK 保持 37。
- 设置归并为六类，支持逐级返回、选项弹层和宽屏双栏；新增主题选择并保留配置重建前的分类。
- 网页视频新增原生全屏控制、亮度/音量/进度手势、长按临时倍速、双击控制、锁定和方向恢复。
- 继续使用 WebView 解码；增强控制按 WebView 能力启用，旧 Provider 的跨域播放器保留网页控件。
- 补齐简体中文、繁体中文和英文资源及 Android 13+ 应用语言配置。
- 修复暂停视频目标丢失、全屏元素替换、临时倍速生命周期和旧系统全屏返回键行为。
- 新增签名 APK 触摸回归和性能对照脚本，实际验证范围见 `EMULATOR_TEST_REPORT.md`。

## 0.1.0 — 2026-09-05

### 功能

- 顶部地址栏聚焦时显示外侧“访问/搜索”按钮，键盘 Go 与按钮共用 URL 分类逻辑。
- 菜单支持添加、编辑和删除书签；默认带入当前页面标题与 URL。
- 菜单按页面操作、浏览数据、隐私与安全、设置与工具四类重新归并。
- 新增 Material 3 原生导航首页；可在设置中切换导航首页与固定网址主页。
- 添加书签时可同时创建带网页 favicon 的首页快捷入口；长按可仅从首页移除。
- HTTPS 安全弹窗显示当前证书使用者、组织、签发机构、有效期和当前有效状态。
- 直播/视频页检测当前实际播放元素，右下角显示悬浮投屏入口；选择页标注“正在播放”。
- 当前视频支持 0.5×–3× 倍速播放，逐帧 WebMessage 控制可覆盖跨域 iframe 播放器。
- 下载目录可选系统 Download 或 SAF 自定义目录，下载线程数可在 1–16 之间设置。
- 新增 HTTP Range 分段下载与 `dataSync` 前台服务，不支持 Range 时自动回退单线程。
- 下载列表支持失败/暂停重试，并可选择仅删除记录或同时删除对应的本地文件。
- Pure 浏览器品牌、清新图标、无痕模式、标签、下载、页面查找和开发者工具完成整合。

### 稳定性与边界

- WebView 池化、renderer 崩溃重建、旧回调隔离和 ActivityResult 回收路径完善。
- 下载/标签/搜索引擎偏好 JSON 有长度和条目上限；文件名、header 和 URL 输入经过清理。
- Cookie 清理等待异步完成回调；SQLite 查询/关闭、DLNA 搜索取消和 WebView 池容量竞态已修复。
- DLNA SOAP/SSDP/设备描述响应采用有界读取，避免局域网设备导致无限内存分配。
- 清除浏览数据增加确认并改为只清理浏览状态，不再删除书签和下载文件。
- 自定义过滤列表持久化、书签草稿、NativeCache 位图和重复提交补齐上限与校验。

### Rust

- 默认构建 `adblock`、`cache`、`url_utils` 三个实际产品模块。
- NativeCache JNI 句柄访问与关闭共用生命周期锁，Rust/Kotlin 两侧均限制 key/entry 大小。
- `downloader`、`filename_parser` 保留为 opt-in legacy；`database` 实验模块隔离，不进 APK。
- Rust workspace 通过 `cargo fmt`、`cargo test` 和 `cargo clippy -- -D warnings`。

### 工程与文档

- 构建、安装、诊断脚本改为基于脚本自身目录，移除旧任务临时路径和全局配置写入。
- 更新 README、架构审查、质量报告、测试报告和交付清单，删除未经验证的性能倍数和旧 APK
  信息。
- 删除重复标签页 UI、未使用的 SSL 包装器和 69 个无用资源；29 处调用统一为 KTX，修复
  安装脚本启动/变量解析，并在 API 34 arm64 模拟器复测最终 APK。
- 移除覆盖整个 Compose/数据层/标签页层的过宽 R8 keep 规则及未使用的 Preview 主依赖，
  对 DEX/native 条目启用直发 APK 压缩；安装包由 9,984,278 bytes 降至 1,898,669 bytes，
  API 34 arm64 模拟器覆盖安装、JNI 导航和视频 2× 播放复测通过。

## 历史记录说明

早期版本曾规划 Rust database、text_search、json_parser 和自建 downloader。这些内容没有
进入当前 workspace 或默认 APK；旧实现如需兼容调用者，必须按 README 中的 opt-in 方式构建，
不能据此推断当前产品已使用这些模块。
