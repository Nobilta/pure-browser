package com.mybrowser.update

import androidx.core.content.FileProvider

/** Android keys providers by component name; keep update URI grants separate from camera capture. */
class UpdateFileProvider : FileProvider()
