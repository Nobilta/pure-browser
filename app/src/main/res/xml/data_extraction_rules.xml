<?xml version="1.0" encoding="utf-8"?>
<!--
  allowBackup is already false, so nothing here should ever run. These rules exist to
  state the intent explicitly rather than rely on a deprecated attribute: a browser's
  data directory holds cookies, session state and cache, none of which should leave
  the device via cloud backup or a device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
