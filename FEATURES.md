# 功能介绍

常用操作与使用注意事项。安装方法见 [README](README.md)，版本变化见[更新日志](CHANGELOG.md)。

## 浏览与界面

- 地址栏可以放在顶部或底部，自动区分网址与搜索词，并给出历史、书签和搜索引擎建议。
- 粘贴的文本里包含网址时，建议列表会直接提供「打开链接」。
- 「在线搜索联想」显示各引擎自己返回的联想词，可随时关闭，无痕模式默认关闭、也可单独开启；
  每个引擎使用自己的接口，不会回退到其他引擎，自定义引擎可以另填联想地址。
- 点击地址栏可以编辑完整网址，旋转屏幕不会丢失未提交的输入；顶部地址栏随页面滚动收起和展开。
- 菜单里可以页内查找、管理标签和打开开发工具。
- 网页等待响应时显示加载进度，可以随时停止；加载失败时显示内置报错页，重试成功后再收起。

### 浏览器全屏

开启后隐藏系统状态栏、导航栏、地址栏和工具栏，网页铺满屏幕；切换、刷新、旋转或重启后保持选择。
右下角的「浏览器操作」浮动按钮可以临时呼出地址栏和工具栏（无痕时保持无痕配色），
呼出后可以编辑地址、前后退、切换标签、打开菜单和管理下载，并提供「继续全屏」和「退出全屏」。
返回手势先关闭弹窗、菜单、输入或查找，没有上层操作时退出浏览器全屏，之后才走网页历史。
系统边缘滑动仍可临时呼出系统栏，网页视频全屏和画中画独立优先。硬件键盘支持 F11 切换、Esc 退出。
保存偏好失败时本次会话仍然退出，并提示下次启动可能恢复原设置。

界面支持跟随系统、浅色和深色主题，Android 12+ 支持动态配色；内置简体中文、繁体中文和英文，
Android 13+ 可以单独设置应用语言。设置支持搜索，宽屏下同时显示分类与详情，
菜单和子页面逐级返回并保留滚动位置。

常用操作：

- 长按链接可以在新标签或后台打开、复制、分享；长按图片可以打开、保存或复制地址。
- 长按返回按钮查看当前标签历史，长按标签按钮新建标签；可以开启底栏滑动切换标签。
- Ctrl+L / T / W / R / F / Tab 分别操作地址栏、标签、刷新和查找。
- 可以设为默认浏览器、接收其他应用分享的网址，或把网站快捷方式添加到手机桌面。

## 标签与返回

标签列表支持搜索、排序、命名分组、缩略图和批量关闭；后台新标签在第一次切过去时才加载。
关闭最后一个标签会回到新标签页，关闭后的普通网页可以从历史记录重新访问。

返回键先走当前网页的历史；历史用完后如果还有其他标签，就关闭当前标签，优先回到来源页，
其次回到最近使用的标签，底栏返回和「×」使用同样的规则。关闭后台标签不改变当前选择，
最后一个标签的系统返回才触发退出确认。

切换标签会暂停旧页面的媒体，并尽量保留页面、表单和滚动位置。页面不设固定的驻留数量，
内存不足时会回收，被回收的页面需要重新加载；同一标签内的后退由 WebView 管理，缓存未命中时也会重建页面。
这些情况下未提交的表单和 JavaScript 内存状态可能丢失。
冷启动默认打开主页，也可以开启恢复普通标签的网址、标题和选中项；无痕标签只在当前会话内保留。

## 首页、书签与历史

主页可以是导航页或固定网址。长按快捷入口可以修改标题、网址、文字或图片图标，也可以删除；
快捷入口与书签分开管理。

书签支持文件夹、排序、移动和批量操作，最多 256 个文件夹、16 层；删除文件夹时内容移到父目录。
可以通过系统文件选择器导入和导出 Netscape HTML，导入前会预览，保留层级和空目录，跳过非法或重复网址；
单次最多 5,000 条，文件不超过 8 MiB。书签和历史都可以搜索，历史按日期分组，可以在新标签打开、复制或删除。

## 扫描二维码

从首页地址栏右侧或菜单进入，可以使用相机、手电筒，也可以选择图片。
识别在本机完成，不需要网络或 Google 服务；拒绝相机权限后仍然可以用图片识别。
HTTP(S) 链接在当前标签打开，没有协议的有效域名会补上 HTTPS，其他结果按原文显示；
Wi-Fi 配置、应用协议、脚本和本地文件地址不会自动执行。离开扫码界面或切到后台会释放相机，
不保存画面或扫码历史。

## 下载与打开文件

下载默认保存到系统 Downloads 目录，也可以授权自定义目录，同名文件自动编号。
每次下载前都会确认文件名、来源和保存位置（网页给出大小时显示，否则注明未知），这是刻意设计，不能关闭。
同一页面自动重复触发的下载会合并处理；确认对话框和自动弹窗预算按标签内的主文档隔离，切换标签后各自恢复。

被拦截、拒绝或已有任务的请求在下载管理中最多保留 5 条，可以逐条手动请求；列表满时新的拦截请求
优先替换已有任务的快捷入口。移除拒绝条目后同一链接可以重新请求，超出保留上限的请求只显示次数，
需要先移除记录再回网页重新触发。同一文档最多自动弹出 3 次确认。
长按「保存图片」不消耗这个预算：已下载过时就地确认「保存新副本」并保留原文件，
正在下载或暂停时只提示状态；取消后可以再次长按。

下载支持暂停、继续、取消、重试，以及只删记录或连同文件删除；点击完成的条目由 Android 选择查看器或安装器。
下载线程可以设为 1–16，默认 4，最多同时运行 3 个任务。「重新下载」创建新副本并保留旧文件，
失败或取消也不会删除旧文件。普通下载会重新读取当前 Cookie，无痕下载不会借用普通会话的登录凭据。
列表显示进度、速度和剩余时间，总大小未知时显示已下载字节；长按进入多选后可以批量删除，
也可以设置只在计费网络之外下载。

普通下载可以在进程重启后恢复，主动暂停的任务保持暂停，无痕下载不跨进程自动恢复；
续传会核对文件是否变化，无法确认是同一文件时完整重下。通知权限在下载设置中按需开启，
拒绝授权不影响下载，只是看不到进度和完成通知。

## 应用更新

默认在应用进程启动时检查一次更新。发现新版后显示版本、大小和发布说明，确认后下载、校验并打开安装器；
启动检查失败不会打扰用户。可以在「设置 → 关于 → 启动时自动检查更新」关闭，也可以用「检查更新」手动操作。
首次更新需要系统的「允许此来源安装」授权，从启动提示发起的更新会在授权返回后自动继续。
更新来自最新正式 GitHub Release，安装前检查文件完整性、版本、设备兼容性和签名，最终由系统安装器确认。
关闭下载对话框会取消下载，调试版不能通过这个入口升级为正式版。

## 视频与投屏

增强播放在全屏时接管网页已经加载的视频，非全屏时保留网站控件。可以在网站设置里单独开关，
保存并刷新后生效；未单独设置的网站跟随全局默认值，接管失败时恢复网页播放器。

- 单击显隐控件，双击中央播放或暂停，双击两侧跳转 10 秒。
- 横滑预览进度，左右竖滑分别调节亮度和音量。
- 显示控件时顶部同时显示时间与电量（在控制栏左侧）；电量低于 20% 且未充电时只有电量转为告警色。
- 倍速面板是一条 0.5×–5× 的滑杆，刻度即 0.1 档。
- 长按临时 2× 加速（可在设置改为 3×），按住继续上滑最高到 5×，松手恢复。
- 视频缓冲停住时显示加载速度。
- 锁定后返回先解锁；退出全屏时恢复方向、亮度和常亮状态。
- 右上角的「画面」菜单可以镜像翻转画面，或把画面拉伸为 3:4、16:9（按该比例变形，不是等比缩放），
  「铺满全屏」则裁掉超出部分把屏幕填满；「还原」回到原始比例。选择按网站保存，在网站设置里同样可以修改。
- 右下角打开倍速或投屏面板，返回或首次点击面板外只关闭面板。

需要网站自带的清晰度、弹幕或字幕控件时，可以关闭该网站的增强播放；
部分跨域播放器受 WebView 限制，只能使用网站原有控件。
普通全屏视频支持画中画和系统媒体控制：后台播放默认关闭，开启后当前普通页可以继续播放，
切换标签或关闭画中画仍会暂停；无痕页面隐藏时暂停，也不提供画中画入口。

DLNA 支持同一局域网内的设备发现、发送地址、播放、暂停、停止、进度和音量控制。
接收器直接读取媒体地址，需要支持对应格式，浏览器不转码，也不代理网页登录凭据；
Blob、DRM 和需要鉴权的媒体可能无法直投，实体接收器的兼容性尚未验证。

投屏面板同时列出网页里嗅探到的视频地址：可以直接投送、复制，也可以下载保存。
清单类地址（`.m3u8`、`.mpd`）只提供投送，因为它们是分片播放列表，下载下来并不是可播放的视频；
Blob 和 MSE 播放的视频没有可直接保存的地址。

## 网站设置与隐私

网站可以单独设置 JavaScript、过滤、图片、第三方 Cookie、桌面模式、网页暗色、增强播放和字号，
以及播放器的画面形状（镜像与 3:4 / 16:9 / 铺满全屏）；
大部分设置按协议、主机和端口分别保存，桌面模式在同站的裸域、m.、mobile.、www. 之间共享。
网站设置文件读不出来时会明确提示并暂停保存，避免显示已保存却在重启后丢失。
权限的「记住」保存失败时，本次仍按用户选择和系统授权处理，并提示未能记住，不会把允许反转为拒绝。
可以在网站设置中确认「重置所有网站设置」以恢复保存，这会清除网站权限和显示偏好，
保留书签、历史和网站数据。

相机、麦克风、定位、受保护媒体标识和外部应用跳转分别询问，网站许可和 Android 系统授权是两个步骤。
网页拍照或录像上传使用系统相机，普通上传使用文件选择器。普通网页可以使用系统 Autofill，
通行密钥还取决于 WebView、凭据提供方及其对浏览器的信任配置。网页登录弹窗会让来源页面保持存活，
弹出和关闭都不丢状态，无痕会话也一样（无痕仍然禁用 Autofill）；Activity 重建或进程退出不能恢复
JavaScript 窗口关系，登录可能需要重试。

可以查看 HTTPS 证书信息，按网站、数据类型和时间段清理数据；实际能清理的内容取决于 WebView，界面会说明范围。

### 无痕模式

支持独立 Profile 的 WebView 会为每次无痕会话创建新的存储空间，退出时清理站点数据和 Cookie。
旧 WebView 会显示共享存储警告：浏览期间可能共享普通登录状态，退出清理也会清除普通 Cookie。

无痕不记录历史，不使用 Autofill、通行密钥或用户脚本。清理期间暂停页面加载和恢复，
Activity 重建或同一进程内重新打开应用仍会等待，失败可以重试。无痕开关退出后保持，
下次启动进入新的无痕会话，不恢复上次的无痕标签、Cookie 或临时授权；需要回到普通模式时在设置中关闭。
**无痕页面可以被截图，也会出现在最近任务预览中。**
主动创建的书签和下载仍会保留，无痕不会隐藏公网 IP。
无痕下载的记录和文件都会保留，但**不保存下载地址的查询参数和来源页面**：这两项常带有会话令牌，
退出无痕后留在应用存储里就等于留下浏览痕迹。记录只保留文件名和实际保存位置，
所以无痕下载在会话结束后只能查看、打开或删除，不能重新下载；新会话也不会用上次会话的下载记录判定重复任务，
包括关闭后在同一进程内重新打开应用。

## 设置导入与导出

设置可以导出为一个 JSON 文件并在其他设备导入，入口在「设置 → 隐私」；导出会等本机订阅配置加载完成。
文件包含界面与浏览偏好、首页、搜索引擎（含自定义）、下载选项、过滤开关与订阅、
各网站的显示偏好，以及书签（含文件夹结构）和浏览历史。

导入前会显示来源版本和影响范围，确认后按分组应用；每组在一次写入中保存，确认成功后才报告导入成功。
非法配置整体拒绝：导入前统一检查订阅地址、重复或内置冲突、容器类型和搜索引擎总量，
异常长的版本、导出时间或订阅标识也会拒绝，预览中的长文本只显示有界摘要。
某个分组失败时会恢复该组原有值并如实提示，不影响其他分组；网站设置损坏时跳过网站分组。
书签和历史按合并方式导入：早已存在的地址保留本机记录，不会删除本机任何书签或历史，
同一个文件导入多次不会重复添加，也不会重复累加访问次数。

导入后会在后台下载已启用自定义订阅缺少的首次规则，已有快照继续使用，停用的订阅不自动下载；
未下载和更新失败会在过滤设置中显示，可以手动重试。自定义下载目录的系统授权无法随文件迁移：
导入「系统下载目录」会切回系统目录，导入「自定义目录」保持本机现有选择，需要时重新授权。

Cookie 与登录状态、网站权限、已下载的文件和下载记录、已安装的用户脚本都不包含在内，
网站权限等安全相关内容永远不会被导出或导入。书签最多 5,000 条、历史最多 20,000 条
（超出时保留最近的记录并在导出后提示省略数量），文件超过 8 MiB 或格式不受支持时整体拒绝。

## 广告过滤与用户脚本

内置 EasyList、EasyPrivacy 和 EasyList China，支持自定义订阅和手动更新，
并在不计流量的网络上每 7 天自动更新一次；更新失败时保留上次有效的规则。
来源和许可见[第三方说明](THIRD_PARTY_NOTICES.md)。
过滤支持常见的网络规则和元素隐藏规则；含 uBlock、AdGuard 扩展语法或 scriptlet 的规则可能不生效。
订阅总量超过引擎能容纳的条数时，设置里会单独说明有多少行因此没有加载（这与「语法不支持」是两回事，
减少订阅即可恢复），开发工具的「过滤命中说明」也会在此时提示结论可能包含未加载的规则。

用户脚本可以从网址、代码、文件或网页链接预览安装，支持启停、删除和手动更新。
支持常见匹配规则、start / end / idle 执行时机、GM 值存储、addStyle、log、openInTab、info，
以及依赖和资源读取。最多安装 24 个脚本，单脚本 1 MiB，源码、依赖和资源合计不超过 12 MiB；
每个脚本最多 8 个依赖和 8 个资源，单个资源不超过 256 KiB，值存储不超过 64 KiB / 256 键。
脚本运行在网页环境里，没有隔离执行环境，也不提供 `GM_xmlhttpRequest` 或任意原生网络、文件接口，
只安装你信任的脚本；旧 WebView 会跳过依赖原生存储桥的脚本，无痕不执行脚本。

## 开发工具

菜单中的开发者工具提供控制台、网络日志、过滤命中说明、命令执行、源码和页面信息，
切换页签会保留草稿、筛选和阅读位置。源码支持 HTML、JavaScript 和 CSS 着色，超过读取上限时提示截断；
网络日志可以按类型和状态筛选、搜索，WebView 没有提供的大小、状态和耗时显示为未知。
