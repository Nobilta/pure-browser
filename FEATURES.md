# 功能介绍

这里介绍常用操作和使用注意事项。安装方法见 [README](README.md)，版本变化见[更新日志](CHANGELOG.md)。

## 浏览与界面

地址栏可以放在顶部或底部，自动识别网址与搜索词，提供历史、书签建议和搜索引擎选择。
粘贴的文本中包含网址时，建议列表会直接给出打开链接的选项；
开启「在线搜索联想」后，输入搜索词时显示搜索引擎返回的联想词，可在设置中关闭，无痕模式默认关闭并可单独开启。
自定义搜索引擎可另填联想地址；百度、Google 和 DuckDuckGo 内置联想，Bing 没有公开联想接口，不会回退到其他引擎。
点击地址栏编辑完整网址，旋转屏幕时保留未提交的输入；顶部地址栏会随页面滚动收起和展开。
菜单中可进行页内查找、管理标签和打开开发工具。
网页等待响应时显示加载进度，可随时停止；加载失败后显示内置报错页，重试期间保留提示，成功显示新页面后再收起。

### 浏览器全屏

菜单和「设置 → 外观」共享同一个「浏览器全屏」开关，开启后隐藏系统状态栏、导航栏、地址栏和工具栏，网页沉浸显示；切换、刷新、旋转或重启后保持选择，且不会重建页面或丢失登录、表单与滚动位置。
右下角保留「浏览器操作」浮动按钮（无痕时保持无痕配色），点击临时呼出地址栏和工具栏，可正常编辑地址、前后退、切标签、开菜单和管理下载；呼出的控制栏提供「继续全屏」（收起控件、保持偏好）和「退出全屏」（关闭偏好）。
返回手势的退路按优先级：先关闭弹窗、菜单、输入或查找，没有上层操作时退出浏览器全屏，恢复普通浏览后才走网页历史。
系统边缘滑动可临时呼出系统栏；网页视频全屏和画中画独立优先，不受浏览器全屏影响。硬件键盘支持 F11 切换、Esc 按上述优先级退出。
退出浏览器全屏立即恢复普通界面；保存偏好失败时，本次会话仍保持退出，并提示下次启动可能恢复原设置。

界面支持跟随系统、浅色和深色主题，Android 12+ 支持动态配色。
内置简体中文、繁体中文和英文；Android 13+ 可以单独设置应用语言，旧系统跟随系统语言。
设置支持搜索，宽屏下可以同时显示分类与详情；菜单和子页面逐级返回并保留滚动位置。

常用操作：

- 长按链接，在新标签或后台打开、复制、分享；长按图片可打开、保存或复制地址。
- 长按返回按钮查看当前标签历史，长按标签按钮新建标签；可开启底栏滑动切换标签。
- 使用 Ctrl+L / T / W / R / F / Tab 操作地址栏、标签、刷新和查找。
- 设为默认浏览器、接收其他应用分享的网址，或将网站快捷方式添加到手机桌面。

## 标签与返回

标签列表支持搜索、排序、命名分组、缩略图和批量关闭。后台新标签在首次切换过去时才加载。
关闭最后一个标签会回到新标签页，关闭后的普通网页可以从历史记录重新访问。

返回键先处理当前网页的历史；历史用完后，若还有其他标签，就关闭当前标签，
优先回到来源页，其次回到最近使用的标签。底栏返回和“×”采用相同的标签选择规则。
关闭后台标签不会改变当前选择，最后一个标签的系统返回才触发退出确认。

切换标签时暂停旧页媒体，并尽量保留页面、表单和滚动位置。
驻留数量没有固定上限，内存不足时会回收页面；被回收的页面需要重新加载。
同一标签内的网页后退由 WebView 管理，后退缓存未命中时也会重建页面。
这些情况下，未提交表单和 JavaScript 内存状态可能丢失。

冷启动默认打开主页，可开启恢复普通标签的网址、标题和选中项。
无痕标签及标签来源关系仅在当前会话内保留。

## 首页、书签与历史

主页可以使用导航页或固定网址。长按快捷入口可修改标题、网址、文字或图片图标，也可删除。
快捷入口与书签分别管理。

书签支持文件夹、排序、移动和批量操作，最多 256 个文件夹、16 层。
删除文件夹会把内容移到父目录。可通过系统文件选择器导入、导出 Netscape HTML；
导入前会预览，保留层级和空目录，跳过非法或重复网址。
单次导入最多 5,000 条，文件不超过 8 MiB。

书签和历史都可搜索。历史按日期分组，可以在新标签打开、复制或删除。

## 扫描二维码

从首页地址栏右侧或菜单进入扫码，可使用相机、手电筒，也可以选择图片。
识别在本机完成，无需 Google 服务或网络；拒绝相机权限后仍可选图识别。

HTTP(S) 链接会在当前标签打开，有效的无协议域名会补上 HTTPS。
其他结果按原文显示，支持选择、复制和继续扫描；Wi-Fi 配置、应用协议、脚本和本地文件地址不会自动执行。
离开扫码界面或切到后台时会释放相机，不保存画面或扫码历史。

## 下载与打开文件

下载默认保存到系统 Downloads 目录，也可授权自定义目录。同名文件自动编号。
每次下载开始前会确认文件名、来源和保存位置，网页提供大小时显示，否则注明未知；这是刻意设计，没有关闭选项。
同一页面重复触发的下载只确认一次，已在进行或已完成的相同地址不会重复开始；
下载确认及自动弹窗预算按标签内的主文档隔离，切换标签后恢复该页面自己的状态。
被拦截、拒绝或已有任务的请求在下载管理中最多保留 5 条，可逐条手动请求；
超出部分显示未保存的请求次数，请先移除记录，再回网页重新触发下载。
新的主文档、关闭标签或结束会话会清理对应请求；同一文档最多自动弹出 3 次确认，拦截及首次溢出分别提示一次。
支持暂停、继续、取消、重试，以及只删记录或连同文件删除。
点击完成条目后由 Android 选择查看器或安装器，必要时显示系统授权和确认。

下载线程可设为 1–16，默认 4；最多同时运行 3 个任务。
“重新下载”创建新副本，保留旧文件和记录；重新下载失败或取消也不会删除旧文件。
普通下载重新读取当前 Cookie，无痕下载不会借用普通会话的登录凭据。
列表显示进度、速度和剩余时间，总大小未知时显示已下载字节。
长按任意一条进入多选，可全选后一次性删除（每项仍单独选择是否删除本地文件）。
可以设置仅在不计流量的网络上下载；通知权限不随首次下载弹出，可在下载设置中按需开启，
拒绝授权不影响下载，只是看不到进度和完成通知。

普通下载可在进程重启后恢复，主动暂停的任务保持暂停；无痕下载不跨进程自动恢复。
续传会核对文件是否变化，无法确认仍是同一文件时会完整重下。

## 应用更新

默认在应用进程启动时检查一次更新。发现新版后显示版本、大小和发布说明，
确认后下载、校验并打开 Android 安装器。启动检查失败时不打扰用户。

可在 **设置 → 关于 → 启动时自动检查更新** 关闭，也可通过 **检查更新** 手动操作。
首次更新需要系统的“允许此来源安装”授权；从启动提示发起的更新会在授权返回后自动继续，
手动入口则按提示分别下载、授权和安装。

更新来自[最新正式 GitHub Release](https://github.com/Nobilta/pure-browser/releases/latest)。
安装前检查文件完整性、版本、设备兼容性和签名，最终由系统安装器确认。
关闭下载对话框会取消下载。调试版不能通过此入口升级为正式版。

## 视频与投屏

增强播放在全屏时控制网页已加载的视频，非全屏时保留网站控件。
可在网站设置中单独开关“增强全屏播放”，保存并刷新后生效；
未单独设置的网站跟随全局默认值。接管失败时恢复网页播放器。

- 单击显隐控件，双击中央播放或暂停，双击两侧跳转 10 秒。
- 横滑预览进度，左右竖滑分别调节亮度和音量。
- 显示控件时在顶部同时显示时间与电量；电量低于 20% 且未充电时只有电量转为告警色，时间保持常色。
- 倍速面板是一条 0.5×–5× 的滑杆，刻度即 0.1 的档位。
- 长按临时使用 2× 加速（可在设置改为 3×）；按住不放继续上滑可提高到最高 5×，松手恢复。
- 视频因缓冲停住时显示加载速度：页面用 MSE 播放时按该视频自身的流量测量，否则退回整个应用的接收速率。
- 锁定后返回先解锁；退出全屏时恢复方向、亮度和常亮状态。
- 右下角打开倍速或投屏面板，返回或首次点击面板外只关闭面板。

需要网站专有的清晰度、弹幕或字幕控件时，可关闭该网站的增强播放。
部分跨域播放器受 WebView 能力限制，只能使用网站原有控件。

普通全屏视频支持画中画和系统媒体控制。后台播放默认关闭，开启后当前普通页可继续播放；
切换标签或关闭画中画仍会暂停视频。无痕页面隐藏时暂停，不发布系统媒体标题，也不提供画中画入口。

DLNA 支持同一局域网内的设备发现、发送媒体地址、播放、暂停、停止、进度和音量控制。
接收器直接读取地址，需支持对应媒体格式；浏览器不转码或代理网页登录凭据。
Blob、DRM 和需要鉴权的媒体可能无法直投。实体接收器兼容性尚未验证。

## 网站设置与隐私

网站可单独设置 JavaScript、过滤、图片、第三方 Cookie、桌面模式、网页暗色、增强播放和字号。
大部分设置按协议、主机和端口分别保存；桌面模式在同站的裸域、m.、mobile.、www. 入口之间共享，
协议或端口不同时仍分别管理。

网站设置文件无法读取时会明确提示，并暂停保存，避免显示已保存却在重启后丢失。
可在网站设置中确认“重置所有网站设置”以恢复保存；这会清除网站权限和显示偏好，保留书签、历史和网站数据。

相机、麦克风、定位、受保护媒体标识和外部应用跳转分别询问；网站许可和 Android 系统授权是两个步骤。
网页拍照或录像上传使用系统相机，普通上传使用文件选择器。
普通网页可使用系统 Autofill，WebView 从创建起绑定当前 Activity；通行密钥还取决于 WebView、凭据提供方及其对浏览器的信任配置。
网页登录弹窗保持来源页面存活，允许其处理消息、请求和跳转，关闭弹窗后回到原页面。
这一行为也适用于无痕会话，但不改变无痕禁用 Autofill 的设置。Activity 重建或进程退出不能恢复 JavaScript 窗口关系，登录可能需要重试。

可查看 HTTPS 证书信息，按网站、数据类型和历史时间段清理数据。
实际可清理的内容取决于 WebView，界面会说明范围。

### 无痕模式

支持独立 Profile 的 WebView 会为每次无痕会话创建新的存储空间，退出时清理站点数据和 Cookie。
旧 WebView 会显示共享存储警告：浏览期间可能共享普通登录状态，退出清理也会清除普通 Cookie。

无痕不记录历史、不使用 Autofill、通行密钥或用户脚本。
无痕开关退出后保持：下次启动直接进入新的无痕会话，不恢复上次的无痕标签、Cookie 或临时授权；
需要回到普通模式时在设置中关闭。
**无痕页面可以被截图，也会出现在最近任务预览中。**
主动创建的书签和下载仍会保留，无痕不会隐藏公网 IP。

## 设置导入与导出

设置可导出为一个 JSON 文件，并在其他设备导入，入口在「设置 → 隐私」。
文件只包含设置白名单：界面与浏览偏好、首页、搜索引擎（含自定义）、下载选项、
过滤开关与订阅，以及各网站的显示偏好。

导入前会显示来源版本和影响范围，确认后按分组应用；每组设置在一次写入中保存，确认保存成功后才报告导入成功。
导入前统一检查订阅地址、重复/内置冲突、容器类型和序列化后的搜索引擎总量，非法配置整体拒绝。
过滤订阅与过滤开关也一起保存；某一分组失败会恢复该组原有值并如实提示，不影响其他分组。
网站设置损坏时跳过网站分组，其余分组照常导入。
自定义下载目录的系统授权无法随文件迁移：导入「系统下载目录」会切回系统目录，
导入「自定义目录」则保持本机现有选择，需要时重新授权。

书签、历史、Cookie、下载文件和应用数据不包含在内；
网站权限等安全相关内容永远不会被导出或导入。文件超过 2 MiB 或格式不受支持时整体拒绝。

## 广告过滤与用户脚本

内置 EasyList、EasyPrivacy 和 EasyList China，支持自定义订阅、手动更新，
以及每 7 天在不计流量网络上自动更新。更新失败时保留上次有效规则。
来源和许可见[第三方说明](THIRD_PARTY_NOTICES.md)。

过滤支持网络规则子集、基础元素隐藏 `##` 和例外 `#@#`。
含 uBlock、AdGuard 扩展语法或 scriptlet 的规则可能无法生效。

用户脚本可从网址、代码、文件或网页链接预览安装，支持启停、删除和手动更新。
支持常见匹配规则、start/end/idle 执行时机、GM 值存储、addStyle、log、openInTab、info 及依赖和资源读取。
最多安装 24 个脚本，单脚本 1 MiB，源码、依赖和资源合计不超过 12 MiB；
每个脚本最多 8 个依赖和 8 个资源，单个资源不超过 256 KiB，值存储不超过 64 KiB / 256 键。

脚本运行于网页环境，没有隔离执行环境，也不提供 `GM_xmlhttpRequest` 或任意原生网络、文件接口。
只安装信任的脚本。旧 WebView 会跳过依赖原生存储桥的脚本，无痕不执行脚本。

## 开发工具

菜单中的开发者工具提供控制台、网络日志、过滤命中说明、命令执行、源码和页面信息。
页签切换保留草稿、筛选和阅读位置，导航后更新为新页面的内容。

源码支持 HTML、JavaScript 和 CSS 着色，超过读取上限时提示截断。
网络日志可按类型和状态筛选、搜索；WebView 没有提供的状态、大小和耗时显示为未知。
