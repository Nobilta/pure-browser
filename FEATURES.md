# 功能介绍

这里介绍 Pure 浏览器目前能做什么，以及使用时需要了解的限制。
安装方法见 [README](README.md)，版本变化见[更新日志](CHANGELOG.md)，测试过的设备和场景见[回归报告](EMULATOR_TEST_REPORT.md)。

## 浏览与界面

支持网页浏览、前进和后退、刷新、页内查找、文件上传、网页弹窗和全屏视频。
地址栏可以放在顶部或底部，自动识别网址与搜索词，提供历史、书签建议和搜索引擎选择。
点击地址栏可编辑完整网址，旋转屏幕时保留未提交的输入；顶部地址栏会随页面滚动收起和展开。

界面使用 Material 3，提供跟随系统、浅色和深色主题，Android 12+ 支持动态配色。
内置简体中文、繁体中文和英文；Android 13+ 可单独设置应用语言，旧系统跟随系统语言。

设置分为浏览与启动、外观、隐私与过滤、下载、视频和关于，支持搜索，宽屏下可同时显示分类与详情。
菜单和子页面逐级返回并保留滚动位置；底部面板位置固定，通过返回、关闭按钮或面板外区域关闭。
设置、书签和扫码使用全屏布局。菜单过渡方式见[动效设计](design/sheet-motion-consistency.md)。

常用操作包括：

- 长按链接，在新标签或后台打开、复制、分享；长按图片可打开、保存或复制地址。
- 长按返回按钮查看当前标签历史，长按标签按钮新建标签；可开启底栏滑动切换标签。
- 使用 Ctrl+L / T / W / R / F / Tab 操作地址栏、标签、刷新和查找。
- 设为系统默认浏览器、接收其他应用分享的网址，或将网站快捷方式添加到手机桌面。

## 标签与返回

标签列表支持搜索、排序、命名分组、缩略图和批量关闭。后台新建的标签在首次切换过去时才加载。
关闭最后一个标签会回到新标签页；目前没有“最近关闭”或撤销关闭功能。

返回键先处理当前网页的历史。历史用完后，如果还有其他标签，就关闭当前标签，优先回到它的来源页，
其次回到最近使用的标签。底栏返回遵循相同规则；点击“×”也会优先回到来源页。
关闭后台标签不会改变当前选择，最后一个标签的系统返回才会触发退出确认。

普通标签切到后台后，浏览器尽量保留其页面、未提交表单、JavaScript 内存状态和滚动位置，并暂停媒体。
驻留数量没有固定上限；创建页面遇到内存不足或收到系统内存压力通知时，按最早停放的顺序回收页面，
最严重的压力通知会清空剩余驻留页。被回收的页面需要重新加载，进程结束后也不能恢复未提交表单。

**同一标签内的网页后退另由 WebView 管理。** 普通模式会在支持时开启后退缓存，
但是否保留文档取决于 WebView 和页面。本次测试的 WebView 145 声明支持，实际仍会重建文档，
导致未提交表单和 JavaScript 内存状态丢失；滚动恢复也受页面和时序影响。
页内锚点与 `pushState` 历史通常不涉及跨文档重建。详见[后退缓存说明](design/back-navigation-without-reload.md)。

冷启动默认打开主页，也可以开启恢复普通标签的网址、标题和选中项。
无痕标签不写入恢复文件，标签来源关系也不跨进程或普通/无痕模式保留。

## 首页、书签与历史

主页可以使用导航页或固定网址。长按快捷入口可修改标题、网址和图标，也可删除；
图标支持文字或系统文件选择器中的图片，导入图片会缩至最长边 96px。快捷入口与书签分别管理。

书签支持文件夹、排序、移动和批量操作。删除文件夹会把内容移到父目录。
可以通过系统文件选择器导入或导出 Netscape HTML 格式；导入前会预览，保留目录层级和空目录，
跳过非法或重复网址，已有书签的标题保持不变。

| 项目 | 限制 |
|---|---|
| 书签文件夹 | 最多 256 个，最深 16 层 |
| HTML 导入 | 最多 5,000 条，文件不超过 8 MiB |
| 书签与历史列表 | 每页 50 条，搜索覆盖全部记录 |

历史按日期分组并合并重复访问，可以打开到新标签、复制或删除。
保存成功后才更新列表，失败时可重试。

## 扫描二维码

从首页地址栏右侧或菜单进入扫码，可以使用相机、手电筒，也可以选择图片。
识别在本机完成，无需 Google 服务或网络连接。

HTTP(S) 网页链接会在当前标签打开，有效的无协议域名会补上 HTTPS。
其他结果按原文显示，支持选择、复制和继续扫描；Wi-Fi 配置、应用协议、`javascript:` 和 `file:` 等内容不会自动执行或跳转。

拒绝相机权限后仍可选图识别。退出扫码、切到后台、展示结果或选图时会释放相机；
不保存相机画面或扫码历史。导入图片按最长边 1600px 采样。

## 下载与打开文件

下载默认保存到系统 Downloads 目录，也可以授权一个自定义目录。同名文件自动编号。
列表显示进度、速度、剩余时间及排队等状态；服务器未提供总大小时，只显示已下载字节和不定进度。

点击已完成的条目，直接交给 Android 打开。系统会选择查看器或安装器，必要时显示应用选择、来源授权或安装确认。
文件已删除、目录授权失效或没有可用应用时，浏览器会提示原因。

下载支持暂停、继续、取消、重试，以及只删记录或连同文件删除。普通下载可以在进程重启后恢复，
主动暂停的任务保持暂停；无痕下载不跨进程自动恢复。
续传会核对文件标识、大小和分段范围，无法确认仍是同一文件时会完整重下。

| 设置或限制 | 当前值 |
|---|---|
| 单任务线程数 | 1–16，默认 4 |
| 同时运行的任务 | 总共 3 个，每主机 2 个 |
| 同时连接数 | 总共 8 个，每主机 4 个，重定向也计入 |
| HTTP 重定向 | 最多 5 次，拒绝 HTTPS 降级 |

可以设置仅在不计流量的网络上下载。网络中断会等待恢复，临时错误会有限重试。
Android 13+ 拒绝通知权限不影响下载；完成通知可打开对应记录，包括应用未运行的情况。

下载使用网页的 User-Agent、Referer 和当前 Profile 的 Cookie。
Cookie 只保存在内存中，重定向时只发给原 origin（协议、主机和端口均相同的来源）。

## 应用更新

默认在应用进程启动时检查一次更新。发现新版后显示版本、包大小和发布说明，
确认后自动下载、校验并打开系统安装器。切换主题等配置变化不会丢失待处理的提示。
可在 **设置 → 关于 → 启动时自动检查更新** 关闭；启动检查遇到离线、限流等失败时不打扰用户。

**设置 → 关于 → 检查更新** 是手动入口，按界面提示分别检查、下载、授权和安装，
失败时会显示对应状态。首次安装更新需要系统的“允许此来源安装”授权；
从启动提示发起的更新会在授权返回后自动继续。

更新来自[最新正式 GitHub Release](https://github.com/Nobilta/pure-browser/releases/latest)的 `update.json`，
成功结果缓存 5 分钟。按 `versionCode` 判断新版，草稿和预发布版不进入此入口。
下载仅允许指定的 GitHub HTTPS 地址，最多 5 次重定向、128 MiB，不携带网页 Cookie 或 GitHub Token。
关闭下载对话框会取消下载并清理未完成文件。

安装前会核对大小、SHA-256、包名、版本、最低 Android 版本、ABI 和签名。
最终仍需在 Android 安装器中确认；不会静默安装或卸载旧应用。调试版不能从此入口升级为正式版。

## 视频与投屏

增强播放只在全屏时尝试接管，控制网页已经加载的视频，继续由 WebView 解码。
非全屏时保留网站原有控件。可在网站设置中单独开关“增强全屏播放”；
未单独设置的网站跟随全局默认值，保存并刷新后生效。

全屏支持播放、暂停、0.5×–3× 倍速、旋转、锁定、画中画和投屏：

- 单击显示或隐藏控件，双击中央播放或暂停，双击两侧跳转 10 秒。
- 横滑预览进度，左右竖滑分别调节亮度和音量。
- 长按临时使用 2× 或 3×，松开恢复原速度。
- 锁定后返回先解锁，退出时恢复屏幕方向、亮度和常亮状态。
- 倍速和投屏面板在右下角打开；返回或首次点击面板外只关闭面板。

接管失败、无法访问视频元素或无法隔离网站控件时，会保留或恢复网页播放器。
需要网站专有的清晰度、弹幕、DOM 字幕等功能时，可关闭该网站的增强播放。
跨域视频控制依赖 WebView 的消息桥等能力，旧版 Provider 可能只能使用网页控件。

普通全屏视频支持画中画和系统媒体控制。后台播放默认关闭，开启后当前普通页可继续播放；
切换标签仍会暂停旧页。关闭画中画会暂停视频。无痕页面隐藏时暂停，不发布系统媒体标题，也不提供画中画入口。
播放结束、离开页面或关闭标签后会清理旧的媒体会话和通知。

DLNA 支持局域网设备发现、发送媒体地址、播放、暂停、停止、进度和设备音量。
候选来自网络请求及已加载视频地址，列表会标记能够匹配的当前播放来源。
接收器直接读取地址，浏览器不代理 Cookie/Referer，也不转码；Blob、DRM、需要鉴权的媒体不保证能直投。

目前没有 Google Cast 支持，也未使用实体接收器确认投屏画面。
详细的控制流程和兼容性说明见[播放器与 DLNA](design/video-playback-and-casting.md)。

## 网站设置与隐私

网站可单独设置 JavaScript、过滤、图片、第三方 Cookie、桌面模式、网页暗色、增强全屏播放和 50%–200% 字号。
桌面视口可用网页原值或 980 / 1024 / 1280 / 1440。
大部分设置按完整 origin 保存；桌面模式在同站、同协议和端口的裸域、m.、mobile.、www. 入口之间共享。

相机、麦克风、定位、受保护媒体标识和外部应用跳转分别管理，网站许可与 Android 系统授权是两个步骤。
网页拍照或录像上传使用系统相机，普通文件上传使用系统选择器；取消操作不会提交旧文件。
普通网页可使用系统 Autofill，WebView 能力和权限满足时启用 WebAuthn；
密码和通行密钥由系统提供方保存。真实账号登录及提供方信任审核不在已验证范围内。

浏览器提供 HTTPS 证书信息、忽略证书后的持续警告、Safe Browsing 和渲染进程崩溃恢复。
同一标签 60 秒内最多自动恢复两次，之后提供手动操作。
清理数据可选择 Profile、数据类型和历史时间段，单站清理会显示实际范围；可清理的内容取决于 WebView 能力。

### 无痕模式

支持独立 Profile 的 WebView 会为每次无痕会话创建新的存储空间。
退出时清理站点数据和 Cookie；暂时无法删除的 Profile 会在下次进程启动时清理，名称不再复用。
清理完成前不能重新进入无痕。

旧 WebView 不支持独立 Profile 时，会显示共享存储警告：浏览期间可能共享普通登录状态，
退出时清理共享站点存储，也会清除普通 Cookie。此模式不记录历史，并禁用 HTTP 缓存。
无痕不使用 Autofill、WebAuthn 或用户脚本。

**无痕页面可以被截图，也会出现在最近任务预览中。**
主动创建的书签和下载仍会保留，无痕也不会隐藏公网 IP。

## 广告过滤与用户脚本

内置 EasyList、EasyPrivacy 和 EasyList China 的 2026-09-09 快照，首次运行即可使用。
支持自定义 HTTP(S) 订阅、手动更新，以及每 7 天在不计流量网络上自动更新。
下载失败或规则无效时保留上次有效规则，来源和许可见[第三方说明](THIRD_PARTY_NOTICES.md)。

过滤支持网络规则子集、基础元素隐藏 `##` 和例外 `#@#`。
第三方判断使用包含 PRIVATE 部分的 Public Suffix List；不完整兼容 uBlock、AdGuard 或 scriptlet。

用户脚本可从网址、代码、文件或网页链接预览安装，支持启停、删除和手动更新。
可用能力包括 `@match/include/exclude/exclude-match/noframes`、start/end/idle、
常用 GM 值存储、addStyle、log、openInTab、info，以及依赖和资源读取。

| 项目 | 限制 |
|---|---|
| 脚本数量 / 单脚本大小 | 24 个 / 1 MiB |
| 源码、依赖和资源合计 | 12 MiB |
| 单脚本依赖与资源 | 最多 8 个 `@require`、8 个 `@resource`，每个资源 256 KiB |
| 单脚本值存储 | 64 KiB、256 个键 |

脚本运行在网页环境，没有隔离执行环境；不提供 `GM_xmlhttpRequest` 或任意原生网络、文件接口。
旧 WebView 只执行加载后的 DOM 脚本，跳过依赖原生存储桥的脚本；无痕不执行脚本。
详细接口和限制见[系统集成说明](design/system-integration.md)。

## 开发工具

菜单中的开发者工具提供控制台、网络日志、过滤规则命中说明、命令执行、源码和页面信息。
页签切换时保留输入草稿、筛选和阅读位置；导航后丢弃旧页面的异步结果。

源码支持 HTML、JavaScript、CSS 着色，最多读取 100 万字符、4096 个文本块，超出时提示截断。
网络日志可按类型、拦截或失败状态筛选，并搜索网址、方法、状态或错误。
日志有容量限制；WebView 未提供的响应状态、大小和耗时会显示为未知。

## 当前范围

目前专注于 Android ARM64 设备，通过 GitHub Releases 分发。以下方向暂不在开发计划内：

- 网页翻译与跨设备同步。
- 完整 uBlock/AdGuard 规则兼容、完整 Tampermonkey API 兼容。
- 非 ARM64 设备、其他操作系统和应用商店分发。
